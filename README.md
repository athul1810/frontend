# Clipify Frontend

AI-powered video clip generator. Turn long videos into viral clips with B-roll, captions, and social-ready exports.

## Structure

```
frontend/
├── clipify/          # Main app (Vite + React)
├── LICENSE
└── README.md
```

## Run

```bash
cd clipify && npm install && npm run dev
```

Open http://localhost:5173
